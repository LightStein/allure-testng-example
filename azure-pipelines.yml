# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
# - task: Bash@3
#   inputs:
#     targetType: 'inline'
#     script: |
#       sudo apt-add-repository ppa:yandex-qatools/allure-framework
      
#       sudo apt-get update
      
#       sudo apt-get install allure-commandline

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
      
      echo '#############'
      
      /home/vsts/work/_tasks/AllureGenerate_**/1.0.0/node_modules/allure-commandline/bin/allure generate --output /home/vsts/work/1/s/target/allure-report/20200817.9 /home/vsts/work/1/s/target/allure-results

  

- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '1.8'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    goals: 'package'
- task: AllureGenerate@1
  inputs:
    resultsDir: 'target/allure-results'
    targetDir: 'target/allure-report/$(Build.BuildNumber)'